# Recursos

- `templates/`: plantillas Excel usadas por los procesos de correo.
- `images/`: logos e imágenes insertados en reportes.

Las rutas se resuelven desde la raíz del repositorio. Si se renombra un archivo, actualice primero
las referencias en los scripts y ejecute las pruebas antes de desplegar.
